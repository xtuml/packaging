This folder contains helper scripts and the generator tools
that are used to build BridgePoint.

The generator can be updated with a newer version from BridgePoint
but the EXE needs to be updated in both the mc3020 and win32 trees
and the schema must be updated as well. 